---
'@puppledoc/nestjs-api-reference': minor
'@puppledoc/space-ui': minor
---

Surface `components.schemas` as a Models group at the bottom of the sidebar — collapsed by default — so DTOs are browsable. Enum properties that reference a named schema now show the model name next to the `enum` label and link to its model page.
