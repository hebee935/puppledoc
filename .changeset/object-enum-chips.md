---
'@puppledoc/nestjs-api-reference': patch
'@puppledoc/space-ui': patch
---

Models list / command palette: classify each schema chip as `OBJECT`, `ENUM`, `ARRAY`, `UNION`, etc. instead of the generic `TYPE`. Detail page keeps its richer `array<X>` label.
