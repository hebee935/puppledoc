---
'@puppledoc/nestjs-api-reference': patch
'@puppledoc/space-ui': patch
---

Show enum members inline under parameter rows and DTO property rows. Previously the UI labeled the field as `enum` but never listed the allowed values, so users had to click through to the model page to see them.
